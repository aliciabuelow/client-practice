export default function RequirementList({ type }) {
    return (
        <div className="RequirementList">
            <h3 className="requirement-title">{type}</h3>
            Requirements
        </div>
    )
}
